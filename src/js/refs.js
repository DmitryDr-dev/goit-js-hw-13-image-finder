const refs = {
  searchForm: document.querySelector('#search-form'),
  galleryList: document.querySelector('.gallery'),
  loadMoreButton: document.querySelector('#load-more'),
  lastElement: document.querySelector('.gallery-item:last-child'),
};

export const { searchForm, galleryList, loadMoreButton, lastElement } = refs;
